import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
        alignSelf: 'center',
        backgroundColor: "#538EFF",
        borderRadius: 10,
        width: "90%",
        marginBottom: 20,
        padding: 15,
    },
});